// import * as message from "./message";

function insertText(): void {
    const elem = document.getElementById("response");
    if (elem !== null) {
        elem.innerText = "lqwrrrrrrt";
    }
}

window.onload = () => {
    insertText();
};
